# C2C 数字交易平台接口文档

## 1. 约定

后端默认地址为 `http://localhost:8080`。除文件下载和支付宝跳转接口外，接口通常返回统一结构：

```json
{
  "status_code": 1,
  "message": "success",
  "data": {}
}
```

`status_code=1` 表示成功，其他值表示业务失败，具体错误信息以 `message` 为准。需要登录的接口通过请求头传递 JWT：

```http
 token: <JWT_TOKEN>
```

分页参数通常使用 `page` 和 `nums`，具体字段以对应 Controller 的参数定义为准。时间格式为 `yyyy-MM-dd HH:mm:ss`。

## 2. 用户与认证

控制器前缀：`/user`

| 方法 | 路径 | 说明 | 典型参数 |
|---|---|---|---|
| POST | `/user/sign-in` | 用户注册 | `accountNumber`、`userPassword`、`nickname` |
| POST | `/user/login` | 用户登录并返回 Token | `accountNumber`、`userPassword` |
| GET | `/user/logout` | 用户退出登录 | 请求头 `token` |
| GET | `/user/info` | 查询当前用户信息 | 请求头 `token` |
| POST | `/user/info` | 更新当前用户资料 | 用户资料对象 |
| GET | `/user/password` | 修改或校验用户密码 | 以 Controller 参数为准 |

## 3. 商品与数字资源

控制器前缀：`/idle`

| 方法 | 路径 | 说明 | 典型参数 |
|---|---|---|---|
| POST | `/idle/add` | 发布商品 | 商品表单、图片和资源信息 |
| GET | `/idle/info` | 查询商品详情 | `id` |
| GET | `/idle/all` | 分页查询商品 | `page`、`nums`、筛选条件 |
| GET | `/idle/find` | 关键词搜索商品 | `findValue`、分页参数 |
| GET | `/idle/label` | 按分类查询商品 | `idleLabel`、分页参数 |
| POST | `/idle/update` | 更新商品 | 商品对象 |
| GET | `/idle/history` | 查看商品修改历史 | `id` |
| GET | `/idle/recommend` | 获取推荐商品 | 分页参数 |

文件控制器提供以下接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/file/file` | 上传图片或普通文件 |
| POST | `/file/uploadResource` | 上传数字资源 ZIP 文件 |
| GET | `/file/image` | 读取图片 | `imageName` |
| GET | `/file/download` | 下载资源文件 | 文件标识或资源路径 |
| GET | `/resource/download` | 受订单权限保护的资源下载 |
| GET | `/tool/check-copyright` | 调用版权检测能力 |

## 4. 收藏、评价和举报

收藏控制器前缀：`/favorite`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/favorite/add` | 收藏商品 |
| GET | `/favorite/delete` | 取消收藏 |
| GET | `/favorite/check` | 检查当前用户是否已收藏 |
| GET | `/favorite/my` | 查询我的收藏 |

评价控制器前缀：`/comment`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/comment/add` | 提交订单评价 |
| GET | `/comment/list` | 查询商品评价列表 |

举报控制器前缀：`/report`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/report/add` | 提交商品或用户举报 |

## 5. 订单、地址与支付

订单控制器前缀：`/order`

| 方法 | 路径 | 说明 | 典型参数 |
|---|---|---|---|
| POST | `/order/add` | 创建订单 | `idleId`、收货/交易信息 |
| GET | `/order/info` | 查询订单详情 | `id` |
| POST | `/order/update` | 更新订单状态 | 订单对象或状态参数 |
| GET | `/order/my` | 查询我买到的订单 | 分页参数 |
| GET | `/order/my-sold` | 查询我卖出的订单 | 分页参数 |

订单地址控制器前缀：`/order-address`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/order-address/add` | 保存订单地址 |
| POST | `/order-address/update` | 更新订单地址 |
| GET | `/order-address/info` | 查询订单地址 |

支付宝控制器前缀：`/alipay`

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/alipay/pay` | 创建支付宝沙箱支付请求 |

## 6. 地址与私信

地址控制器前缀：`/address`

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/address/info` | 查询当前用户地址 |
| POST | `/address/add` | 新增地址 |
| POST | `/address/update` | 更新地址 |
| POST | `/address/delete` | 删除地址 |

用户私信控制器前缀：`/message`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/message/send` | 发送私信 |
| GET | `/message/info` | 查询消息详情 |
| GET | `/message/idle` | 查询商品相关消息 |
| GET | `/message/my` | 查询我的消息 |
| GET | `/message/delete` | 删除消息 |
| GET | `/message/unread/count` | 查询未读消息数 |
| GET | `/message/chat/history` | 查询聊天记录 |
| GET | `/message/chat/list` | 查询会话列表 |

后台客服控制器前缀：`/admin/chat`

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/admin/chat/history` | 管理员查询聊天记录 |
| POST | `/admin/chat/send` | 管理员发送客服消息 |

## 7. 管理员后台

控制器前缀：`/admin`

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/admin/login` | 管理员登录 |
| GET | `/admin/loginOut` | 管理员退出 |
| GET | `/admin/list` | 管理员列表 |
| POST | `/admin/add` | 新增管理员 |
| GET | `/admin/idleList` | 后台商品列表 |
| GET | `/admin/updateIdleStatus` | 更新商品状态 |
| GET | `/admin/queryIdle` | 查询商品 |
| GET | `/admin/orderList` | 后台订单列表 |
| GET | `/admin/deleteOrder` | 删除订单 |
| GET | `/admin/queryOrder` | 查询订单 |
| GET | `/admin/userList` | 后台用户列表 |
| GET | `/admin/updateUserStatus` | 更新用户状态 |
| GET | `/admin/queryUser` | 查询用户 |
| GET | `/admin/auditList` | 待审核商品列表 |
| POST | `/admin/audit` | 审核商品 |
| GET | `/admin/idle/history` | 查询商品历史 |
| GET | `/admin/report/pendingCount` | 待处理举报数量 |
| GET | `/admin/reportList` | 举报列表 |
| POST | `/admin/handleReport` | 处理举报 |
| GET | `/admin/commentList` | 评论列表 |
| POST | `/admin/addComment` | 新增后台评论 |
| POST | `/admin/updateComment` | 更新评论 |
| GET | `/admin/deleteComment` | 删除评论 |
| POST | `/admin/addIdle` | 后台新增商品 |
| POST | `/admin/updateIdle` | 后台更新商品 |
| POST | `/admin/addUser` | 后台新增用户 |
| POST | `/admin/updateUser` | 后台更新用户 |
| GET | `/admin/deleteUser` | 删除用户 |
| POST | `/admin/update` | 更新管理员 |
| GET | `/admin/delete` | 删除管理员 |
| GET | `/admin/idle/pendingCount` | 待审核商品数量 |
| GET | `/admin/repair-hash` | 修复资源哈希 |
| GET | `/admin/sync-stats` | 同步商品统计字段 |
| POST | `/admin/refundOrder` | 订单退款处理 |

## 8. 数据统计

控制器前缀：`/statistics`

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/statistics/trading-data` | 查询交易概览 |
| GET | `/statistics/monthly-statistics` | 查询月度订单和交易金额 |
| GET | `/statistics/category-statistics` | 查询分类统计 |
| GET | `/statistics/user-statistics` | 查询用户统计 |

## 9. 数据模型概览

数据库脚本包含 11 张表：`sh_user` 用户表、`sh_admin` 管理员表、`sh_idle_item` 商品表、`sh_idle_item_history` 商品历史表、`sh_order` 订单表、`sh_order_address` 订单地址表、`sh_address` 用户地址表、`sh_favorite` 收藏表、`sh_comment` 评论表、`sh_message` 私信表和 `sh_report` 举报表。

商品表保存资源路径、原始文件名、解压密码和文件哈希。订单表关联买家、卖家和商品。收藏表通过 `(user_id, idle_id)` 唯一索引防止重复收藏。评论表通过订单和商品关联交易双方，并限制评分为 1 至 5 分。

## 10. 调试示例

### 用户登录

```bash
curl -X POST 'http://localhost:8080/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"accountNumber":"your-account","userPassword":"your-password"}'
```

### 查询商品详情

```bash
curl 'http://localhost:8080/idle/info?id=265'
```

### 带 Token 查询个人信息

```bash
curl 'http://localhost:8080/user/info' \
  -H 'token: YOUR_JWT_TOKEN'
```

接口的精确请求字段、校验规则和返回对象以源码中的 Controller、Model 和 Mapper 为最终依据。生产环境建议补充 OpenAPI/Swagger 规范，并将删除、退款、审核等操作改为更明确的 HTTP 方法和权限校验。

## References

[1]: https://spring.io/projects/spring-boot "Spring Boot 官方项目页面"
[2]: https://mybatis.org/mybatis-3/ "MyBatis 官方文档"
[3]: https://axios-http.com/ "Axios 官方文档"
