<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.second.hand.trading.server.dao.OrderDao">

  <!-- 1. 全量结果映射：包含基础字段、评价标识及财务抽成字段 -->
  <resultMap id="BaseResultMap" type="com.second.hand.trading.server.model.OrderModel">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="order_number" jdbcType="VARCHAR" property="orderNumber" />
    <result column="user_id" jdbcType="BIGINT" property="userId" />
    <result column="idle_id" jdbcType="BIGINT" property="idleId" />
    <result column="order_price" jdbcType="DECIMAL" property="orderPrice" />
    <result column="payment_status" jdbcType="TINYINT" property="paymentStatus" />
    <result column="payment_way" jdbcType="VARCHAR" property="paymentWay" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="payment_time" jdbcType="TIMESTAMP" property="paymentTime" />
    <result column="order_status" jdbcType="TINYINT" property="orderStatus" />
    <result column="is_deleted" jdbcType="TINYINT" property="isDeleted" />
    <result column="comment_flag" jdbcType="INTEGER" property="commentFlag" />
    <result column="commission_price" jdbcType="DECIMAL" property="commissionPrice" />
    <result column="seller_income" jdbcType="DECIMAL" property="sellerIncome" />
  </resultMap>

  <!-- 2. 全量字段列表 -->
  <sql id="Base_Column_List">
    id, order_number, user_id, idle_id, order_price, payment_status, payment_way,
        create_time, payment_time, order_status, is_deleted, comment_flag,
        commission_price, seller_income
  </sql>

  <!-- 3. 基础查询：按主键查询 -->
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" /> from sh_order where id = #{id,jdbcType=BIGINT}
  </select>

  <!-- 4. 买家查询：我的购买记录（过滤已删除订单） -->
  <select id="getMyOrder" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" /> from sh_order
    where user_id = #{userId,jdbcType=BIGINT} and is_deleted = 0
    order by id desc
  </select>

  <!-- 5. 后台管理：获取全量订单列表 -->
  <select id="getAllOrder" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" /> from sh_order order by id desc limit #{begin}, #{nums}
  </select>

  <!-- 6. 后台管理：模糊搜索（订单号或商品名） -->
  <select id="getOrderByNumber" resultMap="BaseResultMap">
    select o.* from sh_order o left join sh_idle_item i on o.idle_id = i.id
    <where>
      <if test="searchValue != null and searchValue != ''">
        and (o.order_number like concat('%', #{searchValue}, '%') or i.idle_name like concat('%', #{searchValue}, '%'))
      </if>
    </where>
    order by o.id desc limit #{begin}, #{nums}
  </select>

  <!-- 7. 后台管理：统计搜索总数 -->
  <select id="countAllOrder" resultType="int">
    select count(*) from sh_order o left join sh_idle_item i on o.idle_id = i.id
    <where>
      <if test="searchValue != null and searchValue != ''">
        and (o.order_number like concat('%', #{searchValue}, '%') or i.idle_name like concat('%', #{searchValue}, '%'))
      </if>
    </where>
  </select>

  <!-- 8. 关联查询 -->
  <select id="findOrderByIdleIdList" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" /> from sh_order where idle_id in
    <foreach collection="list" index="index" item="id" open="(" separator="," close=")"> #{id} </foreach>
    order by id desc
  </select>

  <!-- 9. 删除方法 -->
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from sh_order where id = #{id,jdbcType=BIGINT}
  </delete>

  <!-- 10. 全量插入 -->
  <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.second.hand.trading.server.model.OrderModel" useGeneratedKeys="true">
    insert into sh_order (order_number, user_id, idle_id, order_price,
                          payment_status, payment_way, create_time, payment_time,
                          order_status, is_deleted, comment_flag, commission_price, seller_income)
    values (#{orderNumber,jdbcType=VARCHAR}, #{userId,jdbcType=BIGINT}, #{idleId,jdbcType=BIGINT},
            #{orderPrice,jdbcType=DECIMAL}, #{paymentStatus,jdbcType=TINYINT}, #{paymentWay,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP}, #{paymentTime,jdbcType=TIMESTAMP}, #{orderStatus,jdbcType=TINYINT},
            0, 0, #{commissionPrice,jdbcType=DECIMAL}, #{sellerIncome,jdbcType=DECIMAL})
  </insert>

  <!-- 11. 动态插入 -->
  <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.second.hand.trading.server.model.OrderModel" useGeneratedKeys="true">
    insert into sh_order
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="orderNumber != null">order_number,</if>
      <if test="userId != null">user_id,</if>
      <if test="idleId != null">idle_id,</if>
      <if test="orderPrice != null">order_price,</if>
      <if test="paymentStatus != null">payment_status,</if>
      <if test="paymentWay != null">payment_way,</if>
      <if test="createTime != null">create_time,</if>
      <if test="paymentTime != null">payment_time,</if>
      <if test="orderStatus != null">order_status,</if>
      <if test="commissionPrice != null">commission_price,</if>
      <if test="sellerIncome != null">seller_income,</if>
      is_deleted, comment_flag
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="orderNumber != null">#{orderNumber,jdbcType=VARCHAR},</if>
      <if test="userId != null">#{userId,jdbcType=BIGINT},</if>
      <if test="idleId != null">#{idleId,jdbcType=BIGINT},</if>
      <if test="orderPrice != null">#{orderPrice,jdbcType=DECIMAL},</if>
      <if test="paymentStatus != null">#{paymentStatus,jdbcType=TINYINT},</if>
      <if test="paymentWay != null">#{paymentWay,jdbcType=VARCHAR},</if>
      <if test="createTime != null">#{createTime,jdbcType=TIMESTAMP},</if>
      <if test="paymentTime != null">#{paymentTime,jdbcType=TIMESTAMP},</if>
      <if test="orderStatus != null">#{orderStatus,jdbcType=TINYINT},</if>
      <if test="commissionPrice != null">#{commissionPrice,jdbcType=DECIMAL},</if>
      <if test="sellerIncome != null">#{sellerIncome,jdbcType=DECIMAL},</if>
      0, 0
    </trim>
  </insert>

  <!-- 12. 动态更新 -->
  <update id="updateByPrimaryKeySelective" parameterType="com.second.hand.trading.server.model.OrderModel">
    update sh_order
    <set>
      <if test="orderNumber != null">order_number = #{orderNumber,jdbcType=VARCHAR},</if>
      <if test="userId != null">user_id = #{userId,jdbcType=BIGINT},</if>
      <if test="idleId != null">idle_id = #{idleId,jdbcType=BIGINT},</if>
      <if test="orderPrice != null">order_price = #{orderPrice,jdbcType=DECIMAL},</if>
      <if test="paymentStatus != null">payment_status = #{paymentStatus,jdbcType=TINYINT},</if>
      <if test="paymentWay != null">payment_way = #{paymentWay,jdbcType=VARCHAR},</if>
      <if test="createTime != null">create_time = #{createTime,jdbcType=TIMESTAMP},</if>
      <if test="paymentTime != null">payment_time = #{paymentTime,jdbcType=TIMESTAMP},</if>
      <if test="orderStatus != null">order_status = #{orderStatus,jdbcType=TINYINT},</if>
      <if test="isDeleted != null">is_deleted = #{isDeleted,jdbcType=TINYINT},</if>
      <if test="commentFlag != null">comment_flag = #{commentFlag,jdbcType=INTEGER},</if>
      <if test="commissionPrice != null">commission_price = #{commissionPrice,jdbcType=DECIMAL},</if>
      <if test="sellerIncome != null">seller_income = #{sellerIncome,jdbcType=DECIMAL},</if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>

  <!-- 13. 更新评价状态 -->
  <update id="updateCommentFlag">
    update sh_order set comment_flag = #{flag} where id = #{id}
  </update>

  <!-- 14. 财务统计方法 -->
  <select id="getTotalOrderAmount" resultType="java.math.BigDecimal">
    SELECT IFNULL(SUM(order_price), 0) FROM sh_order WHERE payment_status = 1
  </select>

  <select id="getTotalCommissionAmount" resultType="java.math.BigDecimal">
    SELECT IFNULL(SUM(commission_price), 0) FROM sh_order WHERE payment_status = 1
  </select>

  <select id="getOrderCountByDateRange" resultType="int">
    SELECT COUNT(*) FROM sh_order WHERE payment_status = 1 AND create_time BETWEEN #{startDate} AND #{endDate}
  </select>

  <select id="getOrderAmountByDateRange" resultType="java.math.BigDecimal">
    SELECT IFNULL(SUM(order_price), 0) FROM sh_order WHERE payment_status = 1 AND create_time BETWEEN #{startDate} AND #{endDate}
  </select>
</mapper>